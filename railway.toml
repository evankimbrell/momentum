[build]
builder = "dockerfile"
buildCommand = "cd client && npm install && npm run build && cd ../server && npm install && npx prisma generate && npx prisma migrate deploy"

[deploy]
startCommand = "cd server && NODE_ENV=production npx tsx src/index.ts"
healthcheckPath = "/api/people"
restartPolicyType = "on_failure"
